#include<stdio.h>
int fact(int n);
int main()
{
	int n,i,f;
	printf("enter any integer");
	scanf("%d",&n);
	f=fact(n);
	printf("\n factorial is %d:%d",n,f);
	return 0;
}
int fact(int n)
	{
	if(n==0)
	return(1);
	else
	return(n*fact(n-1));
}
